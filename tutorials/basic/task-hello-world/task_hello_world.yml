---
platform: linux

image_resource:
  type: docker-image
  source: {repository: busybox}

run:
  path: sh
  args:
  - -exc
  - |
    mkdir task-hello-world
    touch testfile
    mv testfile task-hello-world

outputs:
  - name: task-hello-world
