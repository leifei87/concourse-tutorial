---
platform: linux

image_resource:
  type: docker-image
  source: {repository: busybox}

run:
  path: sh
  args:
  - -exc
  - |
    mkdir hello-world
    touch testfile
    mv testfile hello-world

outputs:
  - name: hello-world
